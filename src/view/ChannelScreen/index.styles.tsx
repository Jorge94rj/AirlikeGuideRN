import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    screenContainer: {
        flex: 1,
        backgroundColor: '#000',
        overflow: 'scroll',
        padding: 24
    },
    title: {
        color: '#fff',
        fontSize: 28
    }
});
